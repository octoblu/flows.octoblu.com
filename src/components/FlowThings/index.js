import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const FlowThings = () => {
  return <div>FlowThings</div>
}

FlowThings.propTypes    = propTypes
FlowThings.defaultProps = defaultProps

export default FlowThings
