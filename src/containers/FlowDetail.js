import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

class FlowDetail extends React.Component {
  constructor(props) {
    super(props)

    this.state = {}
  }

  render() {
    return <div>Flow Detail</div>
  }
}

FlowDetail.propTypes    = propTypes
FlowDetail.defaultProps = defaultProps

export default FlowDetail
